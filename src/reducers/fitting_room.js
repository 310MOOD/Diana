import { ACTIONS } from "../constants";

const initialState = {};

// id:
//   {
//     status: "",
//     number: "",
//     userId: ""
//   }

const fittingRoom = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default fittingRoom;

export const selectors = (state = initialState) => ({});
